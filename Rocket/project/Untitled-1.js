let unknown = "<div><div>";
// const checkedDiv = /"</div>"/;
function checkDiv(String) {
    for (let i = 0; i < unknown.length; i++){
        if (unknown = /<div>/){
            if(unknown = /<div>/){
                unknown[i].replace(unknown, "</div>");
                
            }
        }
        unknown+=unknownChecked;
    }
}return unknownChecked
console.log(unknownChecked);
